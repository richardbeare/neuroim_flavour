---
# Configuration file defining python specific libraries on pypi to install
# using easy install
pypi:
  # swift stuff
  - python-keystoneclient
  - python-swiftclient

## compile problems
  #- tables
  #- pygraphviz
  #- pysparse
  #- sympy
